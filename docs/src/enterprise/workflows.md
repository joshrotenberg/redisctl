# Enterprise Workflows

Multi-step operations for common tasks.

## Available Workflows

### init-cluster

Initialize a new cluster with nodes and configuration.

```bash
redisctl enterprise workflow init-cluster \
  --name mycluster \
  --nodes 3
```

TODO: Expand with actual workflow documentation
